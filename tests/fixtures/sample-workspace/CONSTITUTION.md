# Sample Project

## Purpose

A sample workspace for testing.

## Architecture

Source code lives in `src/` directory.

The main entry is `src/index.ts`.

The compiler is at `src/compiler/parser.ts`.

A missing file would be `src/nonexistent.ts`.

## Entry Points

| Command | Description |
|---------|-------------|
| `src/index.ts` | Main entry |
